import java.sql.PreparedStatement;

public class Database {
    public static  boolean createNewProject(Project project){

        String sql = "INSERT INTO project(name, discription, type) VALUES (";

        return false;
    }
}
